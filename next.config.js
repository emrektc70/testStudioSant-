/** @type {import('next').NextConfig} */
const nextConfig = {
  // Ajouter des configurations personnalisées ici
  // Par exemple, configurer des variables d'environnement
  env: {
    MY_API_KEY: "votre_clé_api",
  },
};

module.exports = nextConfig;
